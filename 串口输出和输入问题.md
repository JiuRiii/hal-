串口输出---------------------------------------------------
串口输出时，为了打印到串口中需要重新规定printf的打印方向
int fputc(int ch, FILE *stream)
{
//关键是下面一行代码
//&huart1 告诉用串口1发送数据
// (uint8_t *)&ch  这是需要发的数据地址，原本是32位的，但是一次发送一个字节，因此取这个地址中的低八位发送出去
    HAL_UART_Transmit(&huart1, (uint8_t *)&ch, 1, 10);
    return ch;
}
